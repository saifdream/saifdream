# Portfolio Website — Mohammad Saiful Islam

Static portfolio built from provided resume content.

How to view

1. Open `index.html` in your browser (double-click or use a local server).
2. Replace the avatar image in `index.html` if you want to embed your supplied photo.

Suggested next steps

- Replace placeholder avatar with the provided base64 image or a file `photo.jpg` in same folder.
- Deploy to Vercel / Netlify or host as a static site.
